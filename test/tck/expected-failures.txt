# A2A TCK failures that are known and tracked, not regressions.
#
# `bin/tck all` is red only when the actual failure set differs from this file,
# so a fixed gap is as loud as a new one. Never add a line to silence a
# regression — fix it. When a gap is closed, delete its line in the same commit.
#
# Coupled to TCK_REF in bin/tck: bumping the pin changes node ids, so regenerate
# this file in the same commit.
#
# Comments and blank lines are ignored; order does not matter.


# --- #96: stream events are not the v1.0 StreamResponse oneof -----------------
# The TCK requires `result` to carry exactly one of task/message/statusUpdate/
# artifactUpdate. We emit flat events discriminated by `kind`, so every event in
# a stream is rejected — including the two that do carry `kind`. Unblocked by
# the wire change in #96, not by the snapshot fix alone.
tests/compatibility/core_operations/test_stream_ordering.py::TestStreamEventOrdering::test_streaming_event_ordering[jsonrpc]
tests/compatibility/jsonrpc/test_sse_streaming.py::TestSseStreamingFormat::test_streaming_events_contain_stream_response

# --- #99: tasks/resubscribe is unimplemented ----------------------------------
# STREAM-SUB-004 wants -32001 TaskNotFoundError for an unknown task; the stub
# answers -32004 UnsupportedOperationError for every task. Two node ids, one
# cause. Gated on capabilities.streaming, so declaring it (#84) made these
# mandatory.
tests/compatibility/core_operations/test_requirements.py::test_must_requirement[STREAM-SUB-004-jsonrpc]
tests/compatibility/jsonrpc/test_sse_streaming.py::TestSseSubscribeToTask::test_subscribe_nonexistent_task_returns_error
